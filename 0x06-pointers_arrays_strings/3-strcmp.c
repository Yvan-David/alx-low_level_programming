#include "main.h"
#include <string.h>

/**
 * _strcmp - prints fx
 * @s1: argment
 * @s2: argument
 * Return: string
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
