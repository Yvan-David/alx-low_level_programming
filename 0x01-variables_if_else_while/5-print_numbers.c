#include <stdio.h>
/**
 * main - prints a program that prints the numbers
 * Return: 0 once exited, otherwise non zero
 */
int main(void)
{
int d;
for (d = 0; d < 10; d++)
	printf("%d", d);
printf("\n");
return (0);
}
